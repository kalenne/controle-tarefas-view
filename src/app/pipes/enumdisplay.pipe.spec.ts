import { EnumdisplayPipe } from './enumdisplay.pipe';

describe('EnumdisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumdisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
